 {{!-- Archivo que contiene la estructura del formulario para agregar una NUEVA NOTA --}}

<div class="col-md-4 mx-auto mt-4">
    <div class="card">
        <div class="card-header">
            <h3>New Note</h3>
        </div>
        <div class="card-body">
            <form action="/notes/newNote" method="POST"> {{!-- Dirección en dónde se enviaran los datos escritos --}}
                <div class="form-group">
                    <input type="text" name="title" class="form-control" placeholder="Title" autofocus>
                </div>
                <div class="form-group">
                    <textarea name="description" id="" class="form-control" placeholder="Description"></textarea>
                </div>
                <div class="form-group">
                    {{!-- Botón azul, y block para que ocupe todo el ancho disponible, con submit para enviar petición al server --}}
                    <button class="btn btn-primary btn-block" type="submit"> Save </button>
                </div>
            </form>
        </div>
    </div>
</div>